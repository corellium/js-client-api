/**
 * Corellium API
 * REST API to manage your virtual devices.
 *
 * The version of the OpenAPI document: 7.3.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SnapshotMember from './SnapshotMember';
import SnapshotOwner from './SnapshotOwner';

/**
 * The SnapshotSharing model module.
 * @module model/SnapshotSharing
 * @version 0.4.0
 */
class SnapshotSharing {
    /**
     * Constructs a new <code>SnapshotSharing</code>.
     * 
     * @alias module:model/SnapshotSharing
     * @param sharingType {module:model/SnapshotSharing.SharingTypeEnum} 
     */
    constructor(sharingType) { 
        
        SnapshotSharing.initialize(this, sharingType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, sharingType) { 
        obj['sharingType'] = sharingType;
    }

    /**
     * Constructs a <code>SnapshotSharing</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SnapshotSharing} obj Optional instance to populate.
     * @return {module:model/SnapshotSharing} The populated <code>SnapshotSharing</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SnapshotSharing();

            if (data.hasOwnProperty('sharingType')) {
                obj['sharingType'] = ApiClient.convertToType(data['sharingType'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('sharedBy')) {
                obj['sharedBy'] = SnapshotOwner.constructFromObject(data['sharedBy']);
            }
            if (data.hasOwnProperty('members')) {
                obj['members'] = ApiClient.convertToType(data['members'], [SnapshotMember]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SnapshotSharing</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SnapshotSharing</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SnapshotSharing.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['sharingType'] && !(typeof data['sharingType'] === 'string' || data['sharingType'] instanceof String)) {
            throw new Error("Expected the field `sharingType` to be a primitive type in the JSON string but got " + data['sharingType']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // validate the optional field `sharedBy`
        if (data['sharedBy']) { // data not null
          SnapshotOwner.validateJSON(data['sharedBy']);
        }
        if (data['members']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['members'])) {
                throw new Error("Expected the field `members` to be an array in the JSON data but got " + data['members']);
            }
            // validate the optional field `members` (array)
            for (const item of data['members']) {
                SnapshotMember.validateJSON(item);
            };
        }

        return true;
    }


}

SnapshotSharing.RequiredProperties = ["sharingType"];

/**
 * 
 * @member {module:model/SnapshotSharing.SharingTypeEnum} sharingType
 */
SnapshotSharing.prototype['sharingType'] = undefined;

/**
 * Password with using passwordPublicLink
 * @member {String} password
 */
SnapshotSharing.prototype['password'] = undefined;

/**
 * @member {module:model/SnapshotOwner} sharedBy
 */
SnapshotSharing.prototype['sharedBy'] = undefined;

/**
 * The members who have access to the snapshot
 * @member {Array.<module:model/SnapshotMember>} members
 */
SnapshotSharing.prototype['members'] = undefined;





/**
 * Allowed values for the <code>sharingType</code> property.
 * @enum {String}
 * @readonly
 */
SnapshotSharing['SharingTypeEnum'] = {

    /**
     * value: "publicLink"
     * @const
     */
    "publicLink": "publicLink",

    /**
     * value: "domainRestrictedLink"
     * @const
     */
    "domainRestrictedLink": "domainRestrictedLink",

    /**
     * value: "passwordPublicLink"
     * @const
     */
    "passwordPublicLink": "passwordPublicLink",

    /**
     * value: "emailInvite"
     * @const
     */
    "emailInvite": "emailInvite"
};



export default SnapshotSharing;

