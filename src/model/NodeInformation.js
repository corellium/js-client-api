/**
 * Corellium API
 * REST API to manage your virtual devices.
 *
 * The version of the OpenAPI document: 7.9.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NodeInformation model module.
 * @module model/NodeInformation
 * @version 0.4.3
 */
class NodeInformation {
    /**
     * Constructs a new <code>NodeInformation</code>.
     * 
     * @alias module:model/NodeInformation
     * @param nodeId {String} Unique identifier for the node
     * @param address {String} IP address of the node
     * @param hostname {String} Hostname of the node
     * @param nodeTypes {Array.<String>} Types of services running on this node [compute,controller,gpu]
     */
    constructor(nodeId, address, hostname, nodeTypes) { 
        
        NodeInformation.initialize(this, nodeId, address, hostname, nodeTypes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, nodeId, address, hostname, nodeTypes) { 
        obj['nodeId'] = nodeId;
        obj['address'] = address;
        obj['hostname'] = hostname;
        obj['nodeTypes'] = nodeTypes;
    }

    /**
     * Constructs a <code>NodeInformation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NodeInformation} obj Optional instance to populate.
     * @return {module:model/NodeInformation} The populated <code>NodeInformation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NodeInformation();

            if (data.hasOwnProperty('nodeId')) {
                obj['nodeId'] = ApiClient.convertToType(data['nodeId'], 'String');
            }
            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('hostname')) {
                obj['hostname'] = ApiClient.convertToType(data['hostname'], 'String');
            }
            if (data.hasOwnProperty('sshPort')) {
                obj['sshPort'] = ApiClient.convertToType(data['sshPort'], 'Number');
            }
            if (data.hasOwnProperty('nodeTypes')) {
                obj['nodeTypes'] = ApiClient.convertToType(data['nodeTypes'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NodeInformation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NodeInformation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NodeInformation.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nodeId'] && !(typeof data['nodeId'] === 'string' || data['nodeId'] instanceof String)) {
            throw new Error("Expected the field `nodeId` to be a primitive type in the JSON string but got " + data['nodeId']);
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['hostname'] && !(typeof data['hostname'] === 'string' || data['hostname'] instanceof String)) {
            throw new Error("Expected the field `hostname` to be a primitive type in the JSON string but got " + data['hostname']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['nodeTypes'])) {
            throw new Error("Expected the field `nodeTypes` to be an array in the JSON data but got " + data['nodeTypes']);
        }

        return true;
    }


}

NodeInformation.RequiredProperties = ["nodeId", "address", "hostname", "nodeTypes"];

/**
 * Unique identifier for the node
 * @member {String} nodeId
 */
NodeInformation.prototype['nodeId'] = undefined;

/**
 * IP address of the node
 * @member {String} address
 */
NodeInformation.prototype['address'] = undefined;

/**
 * Hostname of the node
 * @member {String} hostname
 */
NodeInformation.prototype['hostname'] = undefined;

/**
 * SSH port for the node
 * @member {Number} sshPort
 */
NodeInformation.prototype['sshPort'] = undefined;

/**
 * Types of services running on this node [compute,controller,gpu]
 * @member {Array.<String>} nodeTypes
 */
NodeInformation.prototype['nodeTypes'] = undefined;






export default NodeInformation;

