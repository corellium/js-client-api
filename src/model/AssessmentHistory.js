/**
 * Corellium API
 * REST API to manage your virtual devices.
 *
 * The version of the OpenAPI document: 7.9.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AssessmentHistoryLineItem from './AssessmentHistoryLineItem';

/**
 * The AssessmentHistory model module.
 * @module model/AssessmentHistory
 * @version 0.4.3
 */
class AssessmentHistory {
    /**
     * Constructs a new <code>AssessmentHistory</code>.
     * 
     * @alias module:model/AssessmentHistory
     * @param total {Number} Total number of items
     * @param count {Number} Number of items in current response
     * @param limit {Number} Limit used for the request
     * @param offset {Number} Offset used for the request
     * @param results {Array.<module:model/AssessmentHistoryLineItem>} The list of assessments
     */
    constructor(total, count, limit, offset, results) { 
        
        AssessmentHistory.initialize(this, total, count, limit, offset, results);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, total, count, limit, offset, results) { 
        obj['total'] = total;
        obj['count'] = count;
        obj['limit'] = limit;
        obj['offset'] = offset;
        obj['results'] = results;
    }

    /**
     * Constructs a <code>AssessmentHistory</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AssessmentHistory} obj Optional instance to populate.
     * @return {module:model/AssessmentHistory} The populated <code>AssessmentHistory</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AssessmentHistory();

            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [AssessmentHistoryLineItem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AssessmentHistory</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AssessmentHistory</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AssessmentHistory.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                AssessmentHistoryLineItem.validateJSON(item);
            };
        }

        return true;
    }


}

AssessmentHistory.RequiredProperties = ["total", "count", "limit", "offset", "results"];

/**
 * Total number of items
 * @member {Number} total
 */
AssessmentHistory.prototype['total'] = undefined;

/**
 * Number of items in current response
 * @member {Number} count
 */
AssessmentHistory.prototype['count'] = undefined;

/**
 * Limit used for the request
 * @member {Number} limit
 */
AssessmentHistory.prototype['limit'] = undefined;

/**
 * Offset used for the request
 * @member {Number} offset
 */
AssessmentHistory.prototype['offset'] = undefined;

/**
 * The list of assessments
 * @member {Array.<module:model/AssessmentHistoryLineItem>} results
 */
AssessmentHistory.prototype['results'] = undefined;






export default AssessmentHistory;

